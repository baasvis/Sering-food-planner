# Delta vs. 2026-05-02 overnight audit

This file tracks the 110 findings from the 2026-05-02 overnight review against the current tree. Each prior finding is classified as **open** (still present), **partial** (partially addressed), **fixed** (resolved), **stale** (no longer applicable — the code it described is gone), or **can't-tell** (could not be re-verified line-by-line this pass).

## Status histogram

| Status | Count |
| --- | --- |
| Open | 50 |
| Partial | 22 |
| Fixed | 31 |
| Stale | 1 |
| Can't tell | 6 |
| **Total** | **110** |

## Open (50)

| priorId | priorSeverity | title | evidence |
| --- | --- | --- | --- |
| D1 | Critical | package-lock.json gitignored — no reproducible installs | Same as Top12-2: .gitignore:2 + CI npm install (pr-tests.yml:35-37). Unchanged. |
| S1 | Critical | Production and staging Postgres passwords committed to repo | Same as Top12-1: scripts/sync-prod-to-staging.js:7-8 + .claude/launch.json:11,30 unchanged; last edits to sync script predate baseline (commits 7ba3051/b12459e); passwords identical to audit values. No rotation. |
| Top12-1 (S1) | Critical | Rotate the production and staging Postgres passwords / committed DB creds | scripts/sync-prod-to-staging.js:7-8 and .claude/launch.json:11,30 still contain the same working prod+staging postgres passwords; git check-ignore shows .claude/launch.json is NOT gitignored and is git-tracked. No rotation commit since baseline. |
| Top12-2 (D1) | Critical | Commit package-lock.json and switch CI to npm ci | .gitignore:2 still lists package-lock.json; .github/workflows/pr-tests.yml:35,37 still 'npm install' with comment 'No cache: npm — package-lock.json is gitignored in this repo'. |
| T1 | High | stock-location.test.ts re-implements the function under test | test/stock-location.test.ts:9-11 still defines a local getDbStockForLoc with 'Mirrors what is in public/js/orders.ts' comment and tests the copy; the real export at orders.ts:187 is never imported. (batch-recipe-stock-deduct.test.ts now imports its real fn, but stock-location's was not refactored.) |
| A2 | Medium | window [key:string]:any implied across the frontend | types/globals.d.ts:17 still has '[key: string]: any' in the Window interface for the onclick pattern; main.ts still does the giant Object.assign(window,{...}). onclick='' interpolation unchanged. |
| A3 | Medium | Frontend modules are very large and several still growing | All 7 modules still >1000 LOC and most grew vs audit: orders.ts 1991, planner.ts 1838, recipe-editor.ts 1795, dishes.ts 1745, ingredient-db.ts 1466, dashboard.ts 1342, menu-fixer.ts 2193. |
| D4 | Medium | googleapis 128.0.0 is 43 majors behind | package.json:41 still googleapis ^128.0.0; google-auth-library ^9.6.0 (line 40). Transitive uuid CVE concern + drift unchanged. (recipe-sheets legacy still present.) |
| P1 | Medium | dbUpsertCaterings/dbUpsertTransportItems one upsert per row in lock | lib/db.ts:609-618 and 628-637 still 'for (...) await prisma.X.upsert(...)' inside the write path — not batched into one findMany+createMany or $transaction like dbUpsertBatches. |
| P4 | Medium | dbReadAll triggers a denormalize pass on every page load | lib/db.ts:476 dbReadAll still calls denormalizeRecipeIngredients(recipes) (defined 724) — per-row ingredientName/allergens/cost embedded in every /api/data response, not the suggested id->name map. |
| P5 | Medium | loadIngredients over-fetches columns the slim endpoint drops | Same code as A11: routes/ingredients.ts:45-56 findMany() with no select; slim GET projects in JS. |
| P9 | Medium | Frontend bundles all 26 modules upfront via main.ts | main.ts still statically imports every screen module; only ONE dynamic import() exists in the whole frontend (recipe-editor.ts:1007). No screen-level lazy loading wired into the registry/showScreen. |
| S19 | Medium | Hanos allergen detection is English-only heuristic | lib/hanos-client.ts:313 still val.toLowerCase().includes('with') && !includes('without') — no Dutch matching (met/zonder/bevat/vrij van) and no fail-open. Allergen-miss food-safety risk unchanged. |
| U2 | Medium | <button> elements omit type=button | Only 2 of 317 <button template literals carry type="button" (grep counts). Was 4 of 242 at audit; sweep not done. |
| U9 | Medium | Form validation messages inconsistent (inline/alert/silent) | Still a mix: alert() for new-catering name (caterings.ts:237), new-batch name (dishes.ts:1237), feedback (feedback.ts:73); recipe-editor uses toastError (improved there), but no standardized inline-below-field pattern introduced. |
| U10 | Medium | Color contrast: low-contrast --text3 and #888 widespread | base.css:9 --text3 still #a0a09a (~2.4:1); dark mode --text3 #6b6b66 (line 25). The suggested bump to #7a7a72/#909088 was not applied. |
| U20 | Medium | Recipe editor has no unsaved-changes warning on Cancel/close | No dirty/unsaved/Discard-changes/beforeunload/localStorage-draft logic in recipe-editor.ts (grep empty). Closing still discards the draft silently. |
| U21/P20 | Medium | updateIngredientSearch re-renders entire Orders screen per keystroke | ingredient-db.ts:58-62 updateIngredientSearch still calls renderOrders() on every keystroke; input at line 238 oninput=updateIngredientSearch. Split-container pattern not applied — still violates the CLAUDE.md rule. |
| A7 | Low | events.ts SSE registry leaks rows on bad-state writes | events.ts: clients map entry is {res,user} (line 37) — keepAlive interval (33) is NOT stored; broadcast catch only does clients.delete(id) (64) and cannot clearInterval. clearInterval only fires in req.on('close') (42). Orphaned-interval scenario unchanged. |
| A8 | Low | Write-lock pattern is in-process not Postgres-side | lib/db.ts:542-548 withWriteLock is still the in-process Promise chain; no pg_advisory_xact_lock. (Audit explicitly said leave it / document — single-replica.) |
| A11 | Low | loadIngredients over-fetches columns the slim endpoint drops | routes/ingredients.ts:45-56 loadIngredients still prisma.ingredient.findMany() with NO select; GET / mapper (60-82) projects slim in JS after fetching priceHistory/nutrition/targetStock over the wire. |
| A14 | Low | app.ts mounts every router at top level — no graceful degradation note | app.ts:116 still app.use('/api', requireAuth); requireAuth path-skip set is still just /auth/ and /health (routes/auth.ts:311). No documenting comment of bypass paths added. |
| A16/P10 | Low | setInterval in initApp runs rerenderCurrentView every 60s with no leak guard | public/js/init.ts:217 still bare setInterval(...) with no stored id / clearInterval on logout; re-init accumulates timers. |
| A21/S22 | Low | tebi-scraper.js mutates process.env for credential isolation | scripts/tebi-scraper.js:807-836 still sets process.env.TEBI_EMAIL/PASSWORD in try and restores in finally (origEmail/origPass). Not refactored to pass creds as args. |
| D5 | Low | express on 4.x; prisma/@prisma/client on 6.x | package.json:39 express ^4.18.2; :34 @prisma/client ~6.19.2; :47 prisma ~6.19.2. No major bumps. |
| D6 | Low | postinstall downloads ~300MB Chromium even when not needed | package.json:12 postinstall still 'prisma generate && ... playwright install --with-deps chromium'. |
| D7 | Low | engines.node reasonable but CI Node not pinned (no .nvmrc) | No .nvmrc file (ls: No such file or directory). CI still uses node-version '20' resolving to latest 20.x. |
| D8 | Low | Build script chains tsc/vite with no incremental cache | tsconfig.server.json has no incremental/tsBuildInfoFile (grep exit 1). build still 'vite build && tsc -p tsconfig.server.json' (package.json:10). |
| D14 | Low | railpack.json lists 16 apt packages for Chromium | railpack.json still lists 16 aptPackages (libglib2.0-0 ... libasound2) for Playwright/Chromium on the prod container. |
| P3 | Low | recalcRecipeCostsForIngredient runs outside withWriteLock | routes/ingredients.ts:391 still fires recalcRecipeCostsForIngredient(...).catch(...) outside withWriteLock; the recipe.update writes happen unlocked. Only the error-visibility (T5) was added. |
| P6 | Low | Cache-Control max-age=30 short for read-heavy endpoints | routes/recipes.ts:114 recipe detail still private, max-age=30 (suggested 5min not applied). Photo endpoint did get max-age=86400 (line 580). ingredients/full still max-age=30 (ingredients.ts:94). |
| P11 | Low | rebuildPlanner called liberally, O(N) each time | rebuildPlanner still in public/js/core.ts and invoked from many sites (e.g. dishes.ts:538,548,564,885,992); no memoization on S.batches reference equality found. |
| P13 | Low | Recipe versions JSON column grows unbounded | prisma/schema.prisma:332 versions Json @default("[]") still on the Recipe model; no separate RecipeVersion table. Each version save still appends a full snapshot. |
| S10 | Low | Telemetry payload sent to Anthropic includes user emails | lib/ai-analyzer.ts:300-335 prompt body serializes aggregated structures (errors/screenViews/apiPerf/featureUsage) + uniqueUsers count — no explicit per-row userId field, but no redaction guard/unit-test added per the suggested fix; error objects could still carry @-shaped strings. Exposure low/aggregated. |
| S14 | Low | parseCookie builds RegExp from string interpolation | routes/auth.ts:168-171 parseCookie still new RegExp('(?:^\|;\s*)' + name + '=...'); only ever called with literal 'session' so safe, but not hardened. |
| S20/D15 | Low | tebi-error.png written to working directory; not gitignored | scripts/tebi-scraper.js:943 still page.screenshot({path:'tebi-error.png'}) (cwd, not /tmp); .gitignore has no *.png / tebi-error* entry. |
| T6 | Low | dbAppendLog swallows errors | lib/db.ts:646-661 dbAppendLog catch still console.error only — no addBackendEvent('error','log_append_failed',...). Audit-trail failure still silent. |
| T8 | Low | Test cleanup uses Date.now() prefix, not test-isolation | test/api.test.ts:8 still const T = 'test-' + Date.now() + '-'. Mitigated by CI concurrency gate, by-design. |
| T12 | Low | No tests for runDataQualityChecks or aggregateTelemetry | No test file for ai-analyzer / telemetry aggregation (test/ listing has none; recipe-ai-apply-tool.test.ts covers a different module). |
| T15 | Low | e2e coverage-manifest is the right idea but not enforced | weekly-coverage.yml is still the agent-driven mechanism (lines 4,75,79); no hard CI assertion in pr-tests.yml that fails on uncovered trackEvent features. |
| T16 | Low | No idempotency on POST /feedback and /data/patch | No dedupId/idempotency in routes/feedback.ts or public/js/feedback.ts (grep empty). Retried feedback POST still creates duplicate rows. |
| T17 | Low | No retry/telemetry on the AI analysis cron failure | server.ts:97-99 AI analysis cron catch only console.error('Scheduled AI analysis failed', errMsg(e)) — no addBackendEvent('error','ai_analysis_failed',...). (Tebi cron does emit telemetry; AI cron still doesn't.) |
| T21 | Low | Tebi worker exits 0 when one account silently writes 0 rows | scripts/tebi-sync-worker.js:343-349 still exits non-zero only when totalRowsWritten===0 OR failedAccounts===accounts.length; the partial-success (one account 0 rows) case still reported healthy, no tebi_partial_failure event. |
| U5 | Low | Browser confirm()/alert() still used in 8+ places | confirm() at ingredient-db.ts:628,1428, menu-fixer.ts:1354, supplies.ts:386; alert() at auth.ts:166, caterings.ts:237,318,319, dishes.ts:1237, feedback.ts:73, supplies.ts:334,361,427. New supplies.ts added more. |
| U6 | Low | Focus management on screen change is silent | navigate.ts showScreen:72 toggles .active + renderer but no heading/container focus shift on screen change (no tabindex=-1 focus). No change found. |
| U13 | Low | inputmode/autocomplete missing on numeric inputs | grep for 'inputmode' across public/js/*.ts and index.html returns zero matches. |
| U18 | Low | prompt('Version notes:') native dialog | recipe-editor.ts:1230 still const notes = prompt('Version notes (optional):') ?? ''; |
| A24 | Nit | inventoryDone uses non-padded month string format | planner.ts:1185 and 1787 still build `${now.getFullYear()}-${now.getMonth()}-${now.getDate()}` (0-indexed, unpadded) instead of todayIso(). |
| P18 | Nit | inlineEdit does full rebuildPlanner even for name/note | public/js/dishes.ts:555-564 inlineEdit still calls rebuildPlanner() unconditionally regardless of field. |
| T22 | Nit | _lastReport in menu-fixer is a module-level singleton | public/js/menu-fixer.ts:1613 still let _lastReport: ResultsReport \| null = null; indexed by idx in fixMenuGoto/fixMenuAction (1714-1757). |

## Partial (22)

| priorId | priorSeverity | title | evidence |
| --- | --- | --- | --- |
| A1 | High | Frontend TypeScript runs with strict:false / noImplicitAny:false | tsconfig.json:7-8 still strict:false, noImplicitAny:false. BUT the CI-gate sub-issue is fixed: typecheck:client script (package.json:23), typecheck runs both (21), CI runs 'npm run typecheck' (pr-tests.yml:40). |
| Top12-8 (U1/U3) | High | Add aria-live and dialog semantics to toast and modal | Toast has role=status aria-live=polite (index.html:53); save-indicator has role=status aria-live (init.ts:108); modal has role=dialog aria-modal=true tabindex=-1 + focus-on-open (modal.ts:12-25); viewport maximum-scale dropped (index.html:5). BUT no focus-trap and closeModal:28-36 does not restore focus to opener; ARIA still sparse across screens. |
| U1 | High | Zero ARIA attributes anywhere in the frontend | Key surfaces now have ARIA: toast role=status aria-live (index.html:53), save-indicator (init.ts:108), modal role=dialog aria-modal (modal.ts:17), plus init.ts/today-panel.ts/transport-card.ts. Still NOT a comprehensive pass — most screen grids/nav/buttons still lack roles/labels; icon-only FABs unlabelled. |
| U3 | High | Modal pattern doesn't trap focus or restore it | showModal now focuses the dialog on open (modal.ts:22-25) and sets aria-modal. But closeModal:28-36 does NOT capture/restore document.activeElement (comment admits 'leaves implicit'), and there is no Tab focus-trap. |
| A4 | Medium | lib/db.ts conflates four roles incl. recipe pricing | flexPricePer100g + FLEX_PRICE constants extracted to shared/recipe-cost.ts. But calcRecipeCost (db.ts:884), calcRecipeNutrition (db.ts:921) and all validators still live in lib/db.ts which is still 1121 LOC. |
| A12 | Medium | Validation missing at single-entity write boundaries | Bulk POST /api/ingredients now validated via validateIngredients (ingredients.ts:187) — closes the wipe-by-[] for ingredients (T20). But POST /standard-inventory still deleteMany+createMany with no per-row validation (inventory.ts:24-38); POST /feedback no length cap on text (feedback.ts:13-28); POST /prep-checklist still inline deleteMany cleanup (inventory.ts:253-254). |
| A18 | Medium | applySupplierUpdate full bulk-replace of the entire ingredient DB | Frontend still POSTs whole array: ingredient-db.ts:1316 apiPost('/api/ingredients', S.ingredientDb). Backend destructiveness FIXED (T19a): now single INSERT..ON CONFLICT preserving FKs, inside withWriteLock (ingredients.ts:179-233) — no more deleteMany-all FK wipe and lost concurrent edits serialized. But the no-targeted-PATCH architecture is unchanged. |
| P2 | Medium | recalcRecipeCostsForIngredient is N+1 in fire-and-forget path | Single-ingredient recalcRecipeCostsForIngredient (db.ts:1040-1062) still loops calcRecipeCost per recipe (each does its own findMany — N+1). But the new recalcAllRecipeCosts (db.ts:1076-1121, T19) uses one shared price map for the bulk path. So bulk path fixed, per-ingredient path still N+1. |
| P19 | Medium | applySupplierUpdate POSTs the entire ~2100-row DB per upload | Same as A18: frontend still POSTs whole array (ingredient-db.ts:1316); backend no longer deleteMany+createMany — single parameterized INSERT..ON CONFLICT (ingredients.ts:227-231) inside withWriteLock, so write amplification + FK wipe largely mitigated, but no targeted bulk-update endpoint. |
| S7 | Medium | No security headers (CSP, X-Frame-Options, HSTS, etc.) | helmet ^8.1.0 added (package.json:42) and mounted app.ts:34-40 (HSTS, X-Frame SAMEORIGIN, nosniff, Referrer-Policy, COOP same-origin-allow-popups). CSP deliberately still disabled (contentSecurityPolicy:false at app.ts:35) pending the onclick->delegated refactor — so the XSS-reach mitigation CSP would give is not yet in place. |
| T3 | Medium | Critical write paths untested at integration level (SSE broadcast, etc.) | No test references /api/events or broadcast/EventSource (SSE end-to-end still uncovered). xlsx-api-smoke.test.ts and shipment-flow/migration tests added cover some new paths; T20/T18/T24 got dedicated tests. SSE-broadcast and photo-mimetype-rejection still uncovered. |
| T14 | Medium | ingredient-import and tebi-sync have no unit/integration tests | test/xlsx-api-smoke.test.ts now covers the supplier XLSX upload smoke. tebi-sync orchestrator + scraper still have no tests; CSV migrate path still untested. |
| T24 | Medium | Production-mode login flow has zero e2e coverage | Layer 1 FIXED: test/api.test.ts:124-131 asserts COOP is not 'same-origin' (root-cause guard for the 2026-05-04 outage). Layer 2 (production-mode login-screen Playwright project) still deferred — e2e still only exercises dev-login. |
| Top12-10 (D3) | Medium | Dep refresh: @anthropic-ai/sdk + googleapis | @anthropic-ai/sdk fixed to ^0.92.0 (package.json:32, closes GHSA). googleapis STILL ^128.0.0 (package.json:41) — 43 majors behind, transitive uuid CVE concern remains. |
| U7 | Medium | Toast messages disappear on a fixed timer with no manual dismiss | a11y part addressed: toast now role=status aria-live (index.html:53). But error toasts still auto-dismiss at 4000ms with no [x] close button and no role=alert distinction (utils.ts:505). |
| A9 | Low | Renderer registry not enforced; showScreen coupled via dashboard | showScreen MOVED to navigate.ts:72 (dashboard.ts:33 only re-exports for back-compat). But main.ts:11 still imports showScreen from './dashboard', and dashboard.ts still imports many other-screen fns. |
| A13/S9 | Low | safeErrMsg well-designed but inconsistently applied | recipe_cost_recalc_failed now uses addBackendEvent (ingredients.ts:394) but logs raw message. recipes.ts:203,331 still flow raw errMsg(e) into log warn / details. No redaction added to AI prompt body. |
| D11 | Low | @types/multer missing; (req as any).file casts | recipes.ts:531 now uses a typed cast (req as Request & { file?: ... }). But ingredients-import.ts:37 still (req as any).file, and @types/multer is still not a devDependency (types/multer.d.ts shim remains). |
| P12 | Low | loadData fires four secondary loads without awaiting | Cold loaders moved out of loadData into the bootstrap path and now use await Promise.allSettled([loadIngredientDb, loadStorageConfig,...]) (utils.ts:304-306) — race reduced, though storageConfig/kitchenEquipment still lack per-dataset ready events. |
| S12 | Low | Rate limiting only on /api/telemetry | A per-IP throttle was added to /auth/request-access (routes/auth.ts:32-49). But /api/data/patch, photo upload, Hanos add-to-cart, /api/feedback still have no rate limit; no global write limiter. |
| U8 | Low | No skeletons/perceived-loading on most screens | Dashboard placeholder still the main example; orders.ts now has some loading text, but planner/finance still render synchronously off empty S with no first-load signal. No shared showLoadingPlaceholder helper. |
| U23 | Low | Drag start highlights ALL slots incl. past/served | planner.ts:347 batchDragStart now uses '.slot:not(.closed-slot)' (excludes closed slots) but still NOT ':not(.past-slot)' — past slots (class added at line 162) still receive slot-assign-target. |

## Fixed (31)

| priorId | priorSeverity | title | evidence |
| --- | --- | --- | --- |
| D2 | High | xlsx 0.18.5 has known high-severity CVEs with no fix path | package.json:48 xlsx -> CDN 0.20.3 tgz. |
| S2 | High | Stored XSS via id on batches/caterings/transport/recipes | Same as Top12-3: VALID_ID_PATTERN enforced in all entity validators (lib/db.ts:54,128,173,205,995). |
| S3 | High | In dev mode every /api/* request is unauthenticated | Same as Top12-7: boot guard server.ts:50-62 + AUTH_MODE!='production' gating in auth.ts:214,316. |
| T18 | High | brSave stock-deduction silently fails — wrong request body shape | Same as Top12-11: computeStockDeductionUpdates flat array to /api/ingredients/stock/bulk + toastError + test. Audit marked RESOLVED. |
| Top12-3 (S2) | High | Constrain id format on every entity validator to close stored XSS | lib/db.ts:34 defines VALID_ID_PATTERN /^[a-zA-Z0-9_-]{1,200}$/ with comment citing S2; applied in validateBatch:54, validateCatering:128, validateTransportItem:173, validateIngredient:205, validateRecipe:995. esc() also covers attribute interpolation. |
| Top12-4 (D2) | High | Bump xlsx off the High-CVE 0.18.5 | package.json:48 now 'xlsx':'https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz' (the recommended CDN 0.20.3 build, past both advisories). |
| Top12-7 (S3) | High | Refuse to start in production without GOOGLE_CLIENT_ID | server.ts:50-62 boot guard: AUTH_MODE=production exits 1 if GOOGLE_CLIENT_ID empty (and if ALLOWED_EMAILS empty), comment cites S3/S4. routes/auth.ts:214,316 gate dev-mode bypass on AUTH_MODE!='production'. |
| Top12-11 (T18) | High | Fix silent stock-deduction in batch recipe save | recipe-editor.ts:1746-1751 brSave now builds via computeStockDeductionUpdates and POSTs a flat array to /api/ingredients/stock/bulk (route accepts Array.isArray, ingredients.ts:318); failures toastError. Pinned by test/batch-recipe-stock-deduct.test.ts. |
| Top12-12 (A17/U19/A5) | High | Fix the three S.recipeIndex always-empty UX dead-ends | Recipe v1 sunset in S12: no S.recipeIndex anywhere in public/js (only removal comments planner.ts:950, recipes.ts:48,98); RecipeIndex model gone from schema.prisma; /api/recipe-index routes removed (routes/recipes.ts:15); dbReadAll no longer returns recipeIndex. |
| A5 | Medium | Two parallel recipe systems still ship to clients (RecipeIndex) | RecipeIndex model removed from schema.prisma; /api/recipe-index routes removed (routes/recipes.ts:15 'removed in S12'); recipeIndex no longer in dbReadAll return or frontend S. Sunset option (a) taken. |
| A10 | Medium | dbReadAll swallows DB errors and returns empty defaults | lib/db.ts:432-438 explicit comment citing A10/T7, no try/catch, throws. (Same as Top12-5.) |
| D3 | Medium | @anthropic-ai/sdk 0.88.0 on GHSA-p7fg-763f-g4gf | package.json:32 @anthropic-ai/sdk ^0.92.0. Audit marked RESOLVED. |
| S4 | Medium | ALLOWED_EMAILS empty silently allows any Google account | server.ts:57-62 refuses boot if AUTH_MODE=production and ALLOWED_EMAILS empty; runtime auth.ts:227-231 returns 503 in production when empty (fail-closed). Dev/staging keeps warn behavior. |
| S5 | Medium | In-memory session store grows unbounded and never expires | Same as Top12-6: Postgres sessions table + lazy expiry + daily cleanup cron. |
| T2 | Medium | npm run typecheck only typechecks the backend | package.json:21-23 typecheck now runs typecheck:server && typecheck:client (tsc -p tsconfig.json --noEmit); pr-tests.yml:40 runs 'npm run typecheck'. Frontend now gated in CI even though strict is still false. |
| T4 | Medium | Silent-failure pattern in ingredient/order stock saves | Same as Top12-9: persistIngredientStock uses apiPost+toastError (orders.ts:1556-1574). ingredient-db.ts inline stock saves no longer use bare fetch().catch(console.error). |
| T19 | Medium | recalc finds 0 dirty recipes after bulk supplier import | recalcAllRecipeCosts() (lib/db.ts:1076-1121) awaited after bulk POST (routes/ingredients.ts:247) with recipeCostsUpdated surfaced in the response; comment cites T19. Audit marked RESOLVED (PR #37). |
| Top12-5 (A10/T7) | Medium | Stop swallowing DB errors in dbReadAll | lib/db.ts:432-481 dbReadAll has an explicit comment citing A10/T7 and no try/catch; it throws so asyncHandler routes to the global 500 and the frontend shows the data-error banner. |
| Top12-6 (S5) | Medium | Session expiry / cleanup for the in-memory sessions Map | Sessions moved to Postgres 'sessions' table: routes/auth.ts createSession:191-203 with expiresAt; lazy expiry on read getSessionUser:182-187; daily cleanupExpiredSessions cron server.ts:119-127. |
| Top12-9 (T4) | Medium | Replace fire-and-forget stock save with a real error path | orders.ts:1556-1574 persistIngredientStock now uses apiPost+toastError (comment cites T4) with per-(ingredient\|loc) debounce Map; saveGramsPerPiece orders.ts:1539-1545 uses apiPost+toastError. No fetch(...).catch(console.error) stock pattern remains. |
| U4 | Medium | maximum-scale=1.0 in viewport meta blocks pinch-zoom | public/index.html:5 viewport is now 'width=device-width, initial-scale=1.0' — maximum-scale removed. |
| U19 | Medium | 'No recipes in index yet' misleading empty-state | S.recipeIndex removed entirely (see A5/A17); the new-batch modal recipe search now reads v2 recipes. The misleading message string is gone from the codebase. |
| A19 | Low | Module-level singleton timeouts lose updates on fast successive edits | orders.ts:1556 _stockSaveTimeouts is now Map<string,...> keyed by ${ingId}\|${loc} (comment cites A19). Audit already marked RESOLVED 2026-05-03. |
| A22/S21/D16 | Low | prisma/archive scripts are a destructive landmine | prisma/archive/ directory no longer exists (ls: No such file or directory). Audit marked RESOLVED (option 1, deleted). |
| D16 | Low | prisma/archive scripts still in repo (cross-ref A22/S21) | prisma/archive/ directory removed. Audit marked RESOLVED. |
| S6 | Low | Bearer token compared with === (not constant-time) | routes/coverage.ts:35 now uses crypto.timingSafeEqual with a length-equality guard, comment cites S6. |
| S8/S17 | Low | Photo upload trusts client-supplied mimetype (SVG XSS) | routes/recipes.ts:511-521 PHOTO_MIME_WHITELIST (jpeg/png/webp/gif only, SVG rejected), comment cites S8; serve sets X-Content-Type-Options:nosniff (578) + Content-Disposition inline with controlled filename (579). |
| S15 | Low | XLSX upload via xlsx package — prototype-pollution history | Subsumed by D2: xlsx now CDN 0.20.3 (package.json:48), past the prototype-pollution + ReDoS advisories. |
| T5 | Low | Recipe-cost recalculation fire-and-forget; failures only console.error | routes/ingredients.ts:391-398 catch now also addBackendEvent('error','recipe_cost_recalc_failed',{ingredientId,message}); comment cites T5. Audit marked RESOLVED (PR #39). |
| T20 | Low | Bulk supplier-XLSX upload bypasses ingredient validators | routes/ingredients.ts:187 bulk POST now calls validateIngredients(ingredients) (lib/db.ts:253) with per-row charset/length/range/JSON checks; comment cites T20. Audit marked RESOLVED (PR #38). |
| A20 | Nit | Dead duplicate code in saveInlineStock and openStoragePopover | grep for the duplicate '\|\| S.ingredientDb.find(i => i.id === ingredientId)' fallback returns nothing. Audit marked RESOLVED. |

## Stale (1)

| priorId | priorSeverity | title | evidence |
| --- | --- | --- | --- |
| P7 | Low | Missing index on Batch.parentId for child-batch lookups | parentId column was removed in the unified-batch model (schema.prisma:28 lists it among dropped legacy cols + the parent self-FK removed). No parentId relation/query exists, so the finding no longer applies. Batch now @@index([cookDate]) + @@index([recipeId]). |

## Can't tell (6)

| priorId | priorSeverity | title | evidence |
| --- | --- | --- | --- |
| U11 | Medium | Mobile font sizes drop below readable thresholds | Did not exhaustively re-measure every mobile rule; planner.css/mobile.css still carry dense small-font rules. No evidence of a deliberate lift commit. Likely still open but not verified line-by-line. |
| P21 | Low | Tebi scraper uses waitForTimeout(3000) magic numbers | Tebi scraper still browser-driven with hardcoded waits in the general area; did not re-verify the exact line numbers vs audit (file changed). Likely still open; not confirmed line-by-line. |
| P24 | Low | Tebi scraper sequential per-day fetch with no parallelism | tebi-sync-worker.js still iterates dates; did not verify whether a date-range one-shot mode was adopted. Unverified. |
| S18 | Low | Hanos formatProduct returns external imageUrl rendered unchecked | lib/hanos-client.ts still returns imageUrl from Hanos response (formatProduct). Did not exhaustively confirm no frontend now renders it as <img src> nor that an https:// validation was added; audit itself said not exploitable today. Status unverified. |
| U14 | Low | Hard-coded inline colors bypass dark mode | Did not re-verify the specific legend-dot hexes in dishes.ts; finance.ts #888 sites not re-checked line-by-line. No fix commit evidence found; likely still open. |
| U22 | Low | Stocktake save bar uses two equally-prominent buttons | Stocktake flow moved to stocktake.ts per CLAUDE.md; did not re-locate the specific two-button save bar to confirm hierarchy change. Not verified. |
