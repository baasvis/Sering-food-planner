node_modules/
*.bak
data/
.env
dist/

# Local IDE/agent launch config — embeds DB URLs, never commit (audit SEC-1)
.claude/launch.json

# DB snapshots (may contain prod data — never commit)
*-snapshot.json
*-snapshot.sql
pre-unify-snapshot.*

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
guest-history.xlsx
