# Required
DATABASE_URL=postgresql://user:password@localhost:5432/sering

# Google Sign-In (omit for dev mode — auth is bypassed)
GOOGLE_CLIENT_ID=
GOOGLE_CREDENTIALS={}
ALLOWED_EMAILS=user1@example.com,user2@example.com

# Optional
PORT=3000
